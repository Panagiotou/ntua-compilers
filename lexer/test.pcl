program solve;

 label x;
  var numberOfRings : integer;
  var r : real;
  var b : boolean;
  var c : char;
  var k : ^ integer;
  var table : array of char;

  function hanoi (var source, target, auxiliary : array of char;
                       rings : integer):  integer;

      forward procedure move(var source, target : array of char);

      procedure move1(var source, target : array of char);
       begin
        move(target, source)
       end;
      procedure move(var source, target : array of char);
       begin
       move(source, target);
       move1(source, target)
       end;

  begin
    k := @ 4;
    result := k ^;
    move1(auxiliary, auxiliary)
  end;

begin
  x : numberOfRings := 1;
  (*writeString("Please,givethenumberofrings:");*)
  numberOfRings := readInteger();
  writeString("\nHereisthesolution:\n\n");
  writeInteger(numberOfRings);
  writeBoolean(b);
  writeChar(c);
  writeReal(r);
  readString(numberOfRings, table);

  hanoi("left","right","middle",numberOfRings);
  goto x
end.
