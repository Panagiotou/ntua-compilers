program solve;

  var numberOfRings : integer;

  function hanoi (var source, target, auxiliary : array of char;
                       rings : integer) : boolean;

    function move (var source, target : array of char): array of boolean;
    var i : integer;
    var j : array of array of boolean;
     begin
      (*
      writeString("Move from ");
      writeString(source);
      writeString("to");
      writeString(target);
      writeString(".\n")
      *)

      i := 0;
      result := j[i];
     end;

  begin
    if 2 >= 1 then
      begin
        move(source,target);
      end;
    result := move(source, target);
    if (result) then
      begin
        result := false;
      end
  end;

begin
  (*writeString("Please,givethenumberofrings:");*)
  (*numberOfRings := readInteger();*)
  (*writeString("\nHereisthesolution:\n\n");*)
  hanoi("left","right","middle",numberOfRings)
end.
