program solve;

  var numberOfRings : integer;

  function hanoi (var source, target, auxiliary : array of char;
                       rings : integer) : integer;

     function hanoi (var source, target : array of char):array of char;
     begin
      (*
      writeString("Move from ");
      writeString(source);
      writeString("to");
      writeString(target);
      writeString(".\n")
      *)
      result := "sdc"
     end;

  begin
    if rings >= 1 then
      begin
        hanoi(source,target);
      end;
    result := hanoi(source,target)
  end;

begin
  (*writeString("Please,givethenumberofrings:");*)
  (*numberOfRings := readInteger();*)
  (*writeString("\nHereisthesolution:\n\n");*)
  hanoi("left","right","middle",numberOfRings)
end.
